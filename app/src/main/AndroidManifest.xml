<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="onestonetwobirds.capstonuitest3" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".privateHouseKeeping.MainActivity"
            android:label="개인 가계부" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".groupHouseKeeping.GroupActivity"
            android:label="그룹 가계부" >
        </activity>
        <activity
            android:name=".privateHouseKeeping.InsertActivity"
            android:label="가계부 입력" >
        </activity>
    </application>

</manifest>
